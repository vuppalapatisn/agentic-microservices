apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: ecommerce
spec:
  type: LoadBalancer
  selector:
    app: product
  ports:
    - name: http
      port: 8090
      targetPort: 8090
