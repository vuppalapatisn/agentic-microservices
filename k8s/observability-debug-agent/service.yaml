apiVersion: v1
kind: Service
metadata:
  name: observability-debug-agent
  namespace: observability
spec:
  type: ClusterIP
  selector:
    app: observability-debug-agent
  ports:
    - name: http
      port: 8092
      targetPort: 8092
