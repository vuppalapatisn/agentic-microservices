apiVersion: v1
kind: Service
metadata:
  name: observability-server
  namespace: observability
spec:
  selector:
    app: observability-server
  ports:
    - protocol: TCP
      port: 8091
      targetPort: 8091
  type: ClusterIP
