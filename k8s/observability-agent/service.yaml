apiVersion: v1
kind: Service
metadata:
  name: observability-agent
  namespace: observability-agent
spec:
  selector:
    app: observability-agent
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
