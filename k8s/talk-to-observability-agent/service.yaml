apiVersion: v1
kind: Service
metadata:
  name: talk-to-observability-agent
  namespace: observability
spec:
  type: LoadBalancer
  selector:
    app: talk-to-observability-agent
  ports:
    - name: http
      port: 8092
      targetPort: 8092
